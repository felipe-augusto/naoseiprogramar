// wrappers primitivos (embrulho, pacote)

var nome = "Felipe"; // string
// ENGINE START
var temp = new String(nome);
var minisculo = temp.toLowerCase();
temp = null;
// ENGINE END
console.log(minisculo);